<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.testplatform.mapper.ProjectMapper">

    <!-- 查询所有项目，按ID正序 -->
    <select id="findAll" resultType="com.testplatform.entity.Project">
        SELECT id, name, description, status, start_date, end_date, create_time, update_time
        FROM project
        ORDER BY id ASC
    </select>

    <!-- 根据ID查询项目 -->
    <select id="findById" resultType="com.testplatform.entity.Project" parameterType="int">
        SELECT id, name, description, status, start_date, end_date, create_time, update_time
        FROM project
        WHERE id = #{id}
    </select>

</mapper>
