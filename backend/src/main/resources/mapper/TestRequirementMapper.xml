<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.testplatform.mapper.TestRequirementMapper">

    <!-- 根据项目ID查询需求列表，按ID正序 -->
    <select id="findByProjectId" resultType="com.testplatform.entity.TestRequirement">
        SELECT id, req_no, project_id, title, content, priority, status, create_time, update_time
        FROM test_requirement
        WHERE project_id = #{projectId}
        ORDER BY id ASC
    </select>

    <!-- 根据ID查询单个需求 -->
    <select id="findById" resultType="com.testplatform.entity.TestRequirement">
        SELECT id, req_no, project_id, title, content, priority, status, create_time, update_time
        FROM test_requirement
        WHERE id = #{id}
    </select>

</mapper>
