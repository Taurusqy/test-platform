# 数据库设计文档

## 数据库概述

- **数据库类型**：SQLite（嵌入式，无需单独安装）
- **数据库文件**：`backend/data/test_platform.db`（启动时自动创建）
- **建表脚本**：`backend/src/main/resources/db/schema.sql`
- **表数量**：4张核心表
- **表关系**：项目(1) → 需求(N) → 用例(N) → Bug(N)

## ER 关系图

```
project (项目表)
    │ 1
    │
    │ N
test_requirement (测试需求表)
    │ 1
    │
    │ N
test_case (测试用例表)
    │ 1
    │
    │ N
bug (Bug缺陷表)

说明：
- 一个项目包含多个需求
- 一个需求包含多个用例
- 一个用例执行失败可产生多个Bug
- Bug也可直接创建，不关联用例（case_id可为空）
- Bug冗余存储project_id，方便按项目直接查询
```

## 表结构详细设计

### 1. project（项目表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 项目ID，自增主键 |
| name | TEXT | NOT NULL | 项目名称 |
| description | TEXT | - | 项目描述 |
| status | TEXT | DEFAULT '进行中' | 项目状态：进行中/已完成/已暂停 |
| start_date | TEXT | - | 项目开始日期 |
| end_date | TEXT | - | 项目结束日期 |
| create_time | TEXT | DEFAULT datetime('now','localtime') | 创建时间 |
| update_time | TEXT | DEFAULT datetime('now','localtime') | 更新时间 |

### 2. test_requirement（测试需求表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 需求ID |
| req_no | TEXT | - | 需求编号，如 REQ-001（自动生成） |
| project_id | INTEGER | NOT NULL, FOREIGN KEY | 所属项目ID，级联删除 |
| title | TEXT | NOT NULL | 需求标题 |
| content | TEXT | - | 需求详细内容 |
| priority | TEXT | DEFAULT '中' | 优先级：高/中/低 |
| status | TEXT | DEFAULT '待分析' | 需求状态：待分析/分析中/已实现 |
| create_time | TEXT | DEFAULT datetime('now','localtime') | 创建时间 |
| update_time | TEXT | DEFAULT datetime('now','localtime') | 更新时间 |

### 3. test_case（测试用例表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 用例ID |
| case_no | TEXT | - | 用例编号，如 TC-001（自动生成） |
| requirement_id | INTEGER | NOT NULL, FOREIGN KEY | 所属需求ID，级联删除 |
| module | TEXT | - | 所属模块，如：登录模块、购物车模块 |
| title | TEXT | NOT NULL | 用例标题 |
| test_purpose | TEXT | - | 测试目的，一句话说明测什么 |
| precondition | TEXT | - | 前置条件 |
| steps | TEXT | - | 测试步骤（多行文本） |
| expected_result | TEXT | - | 预期结果 |
| priority | TEXT | DEFAULT 'P1' | 优先级：P0(冒烟)/P1(核心)/P2(重要)/P3(次要) |
| case_type | TEXT | DEFAULT '功能测试' | 用例类型：功能测试/性能测试/接口测试/安全测试/兼容性测试/UI测试 |
| status | TEXT | DEFAULT '未执行' | 执行状态：未执行/通过/失败/阻塞 |
| actual_result | TEXT | - | 实际结果（执行时填写） |
| attachment | TEXT | - | 附件，逗号分隔多个文件路径（如 /uploads/a.png,/uploads/b.log） |
| remark | TEXT | - | 备注 |
| executor | TEXT | - | 执行人 |
| execute_time | TEXT | - | 执行时间 |
| create_time | TEXT | DEFAULT datetime('now','localtime') | 创建时间 |
| update_time | TEXT | DEFAULT datetime('now','localtime') | 更新时间 |

**非数据库字段（查询时关联带出）**：
- `relatedBugNos`：关联的Bug编号，逗号分隔（通过 LEFT JOIN + GROUP_CONCAT 查询）

### 4. bug（Bug缺陷表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | BugID |
| bug_no | TEXT | - | Bug编号，如 BUG-001（自动生成） |
| case_id | INTEGER | FOREIGN KEY | 关联的测试用例ID（可为空，允许直接提Bug），删除用例时置空 |
| project_id | INTEGER | NOT NULL, FOREIGN KEY | 所属项目ID（冗余字段，方便按项目查Bug），级联删除 |
| module | TEXT | - | 所属模块 |
| title | TEXT | NOT NULL | Bug标题 |
| description | TEXT | - | Bug详细描述 |
| reproduce_steps | TEXT | - | 重现步骤 |
| expected_result | TEXT | - | 预期结果 |
| actual_result | TEXT | - | 实际结果 |
| environment | TEXT | - | 测试环境，如 Chrome120/Win11/V1.2 |
| affected_version | TEXT | - | 影响版本，如 V1.0、V1.2 |
| bug_type | TEXT | - | Bug类型：代码错误/界面优化/配置问题/安全问题/性能问题/安装部署/其他 |
| attachment | TEXT | - | 附件，逗号分隔多个文件路径 |
| severity | TEXT | DEFAULT '一般' | 严重程度：致命/严重/一般/轻微/建议 |
| bug_priority | TEXT | DEFAULT 'P2' | 优先级：P0(立即)/P1(本版本)/P2(下版本)/P3(有空再改) |
| status | TEXT | DEFAULT '新建' | Bug状态：新建/待修复/已修复/已验证/关闭 |
| reporter | TEXT | - | 提交人 |
| assignee | TEXT | - | 修复人 |
| create_time | TEXT | DEFAULT datetime('now','localtime') | 创建时间 |
| update_time | TEXT | DEFAULT datetime('now','localtime') | 更新时间 |

**非数据库字段（查询时关联带出）**：
- `caseTitle`：关联用例标题（通过 LEFT JOIN test_case 查询）
- `caseNo`：关联用例编号（通过 LEFT JOIN test_case 查询）

## Bug状态流转规则

```
新建 → 待修复 → 已修复 → 已验证 → 关闭
 ↑      ↑        ↑        ↑
 │      │        │        │
 └──────┴────────┴────────┘
  关闭后可重新打开为"新建"
  待修复可打回"新建"
  已修复可打回"待修复"
  已验证不通过可打回"待修复"
```

**合法流转表**：

| 当前状态 | 允许流转到 |
|----------|-----------|
| 新建 | 待修复、关闭 |
| 待修复 | 已修复、新建 |
| 已修复 | 已验证、待修复 |
| 已验证 | 关闭、待修复 |
| 关闭 | 新建 |

非法流转后端返回错误提示，前端也做了对应限制。

## 附件存储设计

- 上传文件保存到 `backend/uploads/` 目录
- 文件命名规则：`UUID_原始文件名`（如 `a1b2c3d4_报错截图.png`）
  - UUID避免重名覆盖
  - 保留原始中文名，显示时去掉UUID前缀
- 数据库 attachment 字段存相对路径，多个附件用逗号分隔
- 通过 `/uploads/**` 静态资源映射访问
- 单文件最大20MB

## 索引设计

SQLite 主键自动建立索引。外键字段（project_id、requirement_id、case_id）在高频查询场景下可考虑添加索引，当前数据量较小暂不额外添加。

## 数据初始化

应用启动时通过 `DataInitializer.java` 检查项目表是否为空，为空则插入示例数据：
- 1个示例项目（电商平台）
- 2个需求（用户登录、购物车）
- 3个测试用例

删除 `backend/data/test_platform.db` 后重启应用，会自动重建数据库和示例数据。
